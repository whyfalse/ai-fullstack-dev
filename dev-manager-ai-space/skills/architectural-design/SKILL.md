---
name: 项目架构设计
description: 根据需求进行模块划分，架构设计，包含数据库设计、接口设计、前后端程序功能设计。
---

##  核心原则
- **契约先行**：任何涉及前后端交互的需求，必须先定义或修改 API 契约。
- **模块划分**：按业务边界和功能独立性进行模块拆分，明确各模块的职责边界，避免跨层耦合。
- **影响优先**：改动前必须评估对数据库、接口、缓存、第三方服务及前端的影响范围。
- **可执行性**：分配给前后端的任务必须具体、可验证，包含明确的输入输出和验收标准。
- **安全与性能**：在设计阶段就考虑鉴权、限流、数据校验、索引优化等非功能需求。
- **数据独立性原则**：数据库设计独立于前端展示需求，基于业务实体关系建模。前端需要的数据格式通过接口层转换，不直接暴露原始表结构。
- **可演进性原则**：接口版本化（`/v1/...`），允许增量变更；数据库设计预留扩展字段（如`extra` JSON），避免频繁改表结构。
- **反模式警告**：避免前后端共享同一个实体类/数据结构；避免在数据库里存储JSON格式的业务核心字段；避免接口返回数据库全部字段
## 注意

- 请务必遵守下面的执行步骤进行需求分析，不要擅自更改目录结构或流程。

## 执行步骤

- 读取需求并在`/requirements/module/`下找到对应的需求模块，理解功能细节、页面设计、验收标准、其他（安全性、质量要求等）。
	
- 该需求可以划分到新的程序模块还是对原有模块进行更改，如果是新模块，创建`/architecture/module/<模块编号>/`文件夹。
	
- 读取`/architecture/contracts/apis/`下现有的api定义，分析是否需要新增/修改 API。
	
- 读取`/architecture/contract/database/`下现有的数据库定义，作为数据库表设计的参考。
	
- 识别出前端、后端程序功能。
	
- 输出设计概要（包含模块归属、API、数据库、程序逻辑、变更等信息），人工确认，若通过，继续后续流程，未通过则根据反馈重新分析。
	
- 将新api接口更新到`/architecture/contracts/apis/interfaces/`，每个接口对应一个文件，将接口出入参定义更新到`/architecture/contracts/apis/params/`下，每个数据模型对应一个文件。最后更新`/architecture/contracts/apis/catalog.md`目录。示例请参照`/appendix/接口契约模板/`文件夹下对应文件，注意：严格按照示例文件的格式进行编写，不得额外添加内容。
	
- 将变更后的数据库表写入到`/architecture/contracts/database/xxx.sql`，并更新目录`/architecture/contracts/database/catalog.md`。示例请参照`/appendix/数据库契约模板/`文件夹下文件，注意，严格按照示例文件的格式进行编写，不得额外添加内容。
	
- 生成前端程序功能，细化到页面/组件/样式/状态管理/API 调用，注意：不要写具体的实现代码。
	
- 将前端程序功能写入到`/architecture/module/<模块编号>/frontend.md`
	
- 生成后端程序功能，细化到控制器/服务/数据访问/单元测试，注意：不要写具体的实现代码。
	
- 将后端程序功能写入到`/architecture/module/<模块编号>/backend.md`。
	
- 变更内容写入到`/architecture/module/<模块编号>/change-log/<日志编号>.md`，编号规则为日期+序号（例：20260522-0）。
	
- 总结出变更概览。
	